Commits:


git add -A
git commit -m "describe what changed"
git push


Run:

cd "/Users/basiltoufexis/Desktop/TF-F6 App/scoreboard_flutter"
flutter clean
flutter pub get
flutter run


Update:

Before every release, open pubspec.yaml and bump the version:

version: 1.0.0+1
#        ↑ name shown in store  ↑ versionCode (increment every upload)

The number after + must go up by 1 every single time you upload to Play Store. Start at +1, next upload +2, etc.