<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
    "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    SCOREBOARD FLUTTER — iOS Info.plist additions
    These keys are required for Local Network access on iOS 14+.
    Merge these into your existing Info.plist generated by flutter create,
    or use this as a reference to add entries manually in Xcode.
-->
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>Scoreboard</string>

    <key>CFBundleDisplayName</key>
    <string>Scoreboard</string>

    <!-- ── Local Network permission (iOS 14+) ──────────────────────────────── -->
    <!-- Required to send UDP packets to 192.168.1.252 on local network       -->
    <key>NSLocalNetworkUsageDescription</key>
    <string>Required to communicate with the LED scoreboard controller on your local network.</string>

    <!-- Bonjour services declaration — required alongside NSLocalNetworkUsageDescription -->
    <key>NSBonjourServices</key>
    <array>
        <string>_scoreboard._udp</string>
    </array>

    <!-- ── App Transport Security (allow arbitrary UDP on local network) ───── -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsLocalNetworking</key>
        <true/>
    </dict>

    <!-- ── Standard Flutter keys ────────────────────────────────────────────── -->
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <key>UIMainStoryboardFile</key>
    <string>Main</string>

    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
    </array>

    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <key>CADisableMinimumFrameDurationOnPhone</key>
    <true/>

    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>

</dict>
</plist>
